name: lego_parts_counter
description: Lego parts counter

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  http: ^0.12.0
  cupertino_icons: ^0.1.2
  shared_preferences: ^0.5.2
  url_launcher: ^5.4.2

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:

  uses-material-design: true
